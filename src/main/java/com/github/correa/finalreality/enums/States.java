package com.github.correa.finalreality.enums;

/**
 * Holds the types of states.
 */
public enum States {
  BEGIN_TURN_STATE, END_GAME_STATE, END_TURN_STATE, ON_TURN_STATE, SELECTING_TARGET_STATE, START_GAME_STATE, WAITING_STATE
}
